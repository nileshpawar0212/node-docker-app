apiVersion: v1
kind: Service
metadata:
  name: node-docker-app-service
  namespace: node-docker-app
spec:
  type: LoadBalancer   # or NodePort if on bare metal
  selector:
    app: node-docker-app
  ports:
    - port: 80
      targetPort: 3000

